package com.dropbox.componentbox.samples.counter

import androidx.compose.runtime.Composable
import com.dropbox.componentbox.Component

class RenderingEngine {
    @Composable
    operator fun invoke(builder: () -> Component?) {
        // TODO()
    }
}