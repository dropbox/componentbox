<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.dropbox.componentbox.discovery.android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name="com.dropbox.componentbox.discovery.discovery.DiscoveryApp"
        android:label="Discovery App"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light" android:icon="@mipmap/icon" android:roundIcon="@mipmap/icon_round"
        android:usesCleartextTraffic="true">

        <activity android:name="com.dropbox.componentbox.discovery.discovery.DiscoveryActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>